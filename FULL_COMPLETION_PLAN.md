# خطة إكمال بناء تطبيق زمام POS والتطبيقات المرتبطة (خطة شاملة ومفصلة)
> **تاريخ التحديث:** 2026-07-26  
> **الهدف:** توفير دليل عمل تفصيلي ودقيق لأي وكيل ذكاء اصطناعي (AI Agent) أو مطور لإكمال ربط تطبيقات زمام POS بالباك إند (Backend) وإنهاء الميزات المتبقية بنسبة 100%.

---

## 📋 1. ملخص بنية المشروع والحالة الحالية

المشروع عبارة عن نظام نقاط بيع (POS) متكامل يخدم المطاعم والمتاجر، ويتكون من الأنظمة التالية:

1. **`backend`**: خادم Express.js / Node.js يعتمد على MySQL عبر Sequelize ORM، يحتوي على 34 مسار API (Routes) تغطي المبيعات، المخزون، الحسابات، المشتريات، الموارد البشرية، والتوصيل.
2. **`flutter_pos_app`**: تطبيق Flutter لتطبيقات نقطة البيع (Android / iOS / Desktop) يعمل باللغة العربية بوضع RTL.
3. **`flutter_rider_app`**: تطبيق Flutter مخصص لكباتن التوصيل (Riders).
4. **`pos` & `website` & `kds`**: الواجهات المباشرة للويب (React/HTML/JS) لنقطة البيع، شاشة المطبخ (KDS)، ولوحة الإدارة (Admin Dashboard).

---

## 🎯 2. حالة الإنجاز الحالية في `flutter_pos_app`

### ✅ الميزات والمكونات المكتملة والمجربة:
* **مصادقة المستخدمين (Auth)**: شاشة تسجيل الدخول (`login_screen.dart`) والربط مع backend API وتخزين التوكين في `FlutterSecureStorage`.
* **الهيكل الرئيسي والتنقل (Navigation & Shell)**:
  * `home_shell.dart`: يحتوي على الشريط السفلي وشريط التنقل الجانبي (Drawer).
  * تم حل مشكلة اختفاء وإغلاق القائمة الجانبية (Drawer) وتمرير `Builder` لجميع الشاشات.
  * إخفاء الـ Bottom Navigation Bar عند فتح شاشة الإعدادات لمنع أخطاء Index out of bounds.
* **شاشة المبيعات والكاشير (`pos_main_screen.dart`)**:
  * عرض وجبات المنيو والتصنيفات ديناميكياً من الباك إند.
  * سلة المبيعات (Cart) مع إمكانية إضافة وتعديل الكميات وإغلاق الطلب.
  * خيارات نوع الطلب (صالة / سفري / توصيل).
  * شاشة إتمام الدفع (`checkout_modal.dart`).
* **شاشة الإعدادات (`settings_screen.dart`)**:
  * ربط `SettingsRepository` مع `/api/settings` لاسترجاع وحفظ بيانات المنشأة، الضريبة، سير العمل، وسياسات التشغيل.
* **شاشة التقارير (`reports_screen.dart`)**:
  * التقرير اليومي، تقرير المبيعات حسب الفترة، الأكثر مبيعاً، وتقرير الورديات.
* **شاشة الموردين والمشتريات (`purchases_screen.dart`)**:
  * عرض فواتير الشراء، أوامر الشراء، الموردين، وإضافة شراء مباشر.
* **شاشة المخزون والمستودعات (`inventory_screen.dart`)**:
  * دليل المستودعات، الجرد والتحويلات والتنبيهات.

---

## ⚠️ 3. الثغرات والأجزاء المتبقية للربط بالباك إند (Detailed API Gap Matrix)

يجب على الأيجنت المكمل التركيز على ربط الشاشات التالية بالباك إند لضمان تفاعل حي وكامل (Live Backend Integration):

### أ. الشاشات ذات الربط الجزئي / أو المحاكاة (Mock Data):

#### 1. شاشة إدارة الورديات (`shift_screen.dart` / `ShiftRepository`)
- **المطلوب:**
  - ربط قراءة وردية الكاشير الحالية مع مسار الباك إند: `GET /api/shifts/current`
  - تنفيذ فتح الوردية (`POST /api/shifts/open`) مع إدخال مبلغ البداية في الصندوق (Drawer Float).
  - تنفيذ إغلاق الوردية (`POST /api/shifts/close`) مع إدخال مبالغ العد الفعلي (Cash/Card Count) واستخراج عجز/زيادة الصندوق وتوليد تقرير X-Report و Z-Report.
- **تحديث النموذج:** التأكد من توافق `ShiftModel` مع حقول الجدول في MySQL:
  ```json
  {
    "id": "uuid",
    "userId": "uuid",
    "startTime": "datetime",
    "endTime": "datetime",
    "openingBalance": 500.0,
    "closingBalance": 1250.0,
    "actualCash": 1240.0,
    "difference": -10.0,
    "status": "OPEN|CLOSED"
  }
  ```

#### 2. شاشة الطاولات والصالة (`tables_screen.dart` / `TableRepository`)
- **المطلوب:**
  - الربط الحي مع مسار الطاولات: `GET /api/tables` و `POST /api/tables`
  - دعم نقل الطلب بين الطاولات (`POST /api/tables/:id/transfer`)
  - دعم دمج طاولتين أو تقسيم الفاتورة (Split Bill / Merge Tables).
  - الربط مع Socket.io لتحديث حالة الطاولة فورياً (متاحة، مشغولة، حجز، تنظيف) بين الكاشير والويتر.

#### 3. شاشة إدارة العملاء ونظام الولاء (`customers_screen.dart` / `CustomersRepository`)
- **المطلوب:**
  - الربط الكامل لمسارات العملاء: `GET /api/customers` و `POST /api/customers` و `PUT /api/customers/:id`
  - ربط نقاط الولاء والاستبدال مع الباك إند عبر `GET /api/loyalty` و `POST /api/loyalty/redeem`
  - عرض كشف حساب العميل والديون المستحقة عند البيع الآجل (Credit Sales).

#### 4. شاشة المطبخ والشاشات المساندة (KDS Integration)
- **المطلوب:**
  - دعم إرسال الطلبات مباشرة إلى شاشة المطبخ (`kds`) عبر WebSockets أو HTTP Post عند الضغط على "إرسال للمطبخ".
  - تحديث حالة الوجبة (جاري التحضير -> جاهز -> تم التسليم).

---

## 🚚 4. خطة إنهاء تطبيق الكابتن والتوصيل (`flutter_rider_app`)

تطبيق الكابتن موجود في مجلد `flutter_rider_app` ويتطلب التكميل الكامل كالتالي:

1. **مصادقة الكابتن (Rider Login)**:
   - تسجيل دخول الكابتن برقم الهاتف أو اسم المستخدم (`POST /api/auth/login`).
2. **قائمة طلبات التوصيل المتاحة والنشطة**:
   - جلب طلبات التوصيل الخاصة بالكابتن: `GET /api/delivery/rider-orders`
   - قبول الطلب وتغيير حالته (تم الاستلام من المطعم -> جاري التوصيل -> تم التوصيل).
3. **التكامل مع الخريطة والاتجاهات**:
   - إرسال موقع الكابتن الحالي (GPS coordinates) دورياً للباك إند عبر Socket.io (`rider:location_update`).
   - عرض موقع العميل ورقم الهاتف والتواصل المباشر.
4. **إغلاق الحساب المالي للكابتن**:
   - عرض إجمالي المبالغ المحصلة كاش من العملاء والمبلغ الواجب تسليمه للكاشير/الخزينة.

---

## 🖨️ 5. دمج الطباعة الحرارية وطباعة الفواتير (Thermal Printers Integration)

حسب متطلبات نقاط البيع، يجب إضافة دعم الطباعة المباشرة من تطبيق Flutter:

1. **إضافة الحزم (Packages) في `pubspec.yaml`**:
   - `esc_pos_printer` أو `flutter_pos_printer_platform`
   - `blue_thermal_printer` (للطابعات المحمولة عبر البلوتوث).
2. **أنواع الطباعة المطلوب دعمها**:
   - **فاتورة العميل (Customer Receipt)**: تشمل اسم المتجر، الرقم الضريبي، كود QR الخاص بـ زكاتك (ZATCA QR code)، الأصناف، والضريبة.
   - **أمر المطبخ (Kitchen Order Ticket - KOT)**: يرسل للمطبخ مقتصراً على الأصناف والملاحظات (بدون أسعار).
   - **تقرير الوردية (X/Z Report)**: يطبع عند إغلاق الوردية من شاشة الورديات.

---

## 🔄 6. الدعم اللاسلكي والأوفلاين (Offline First Architecture)

لضمان عدم توقف الكاشير عند انقطاع الإنترنت:

1. **التخزين المحلي للطلبات (Local Database)**:
   - استخدام `sqflite` أو `hive` في `flutter_pos_app` لتخزين الوجبات والتصنيفات محلياً.
2. **سجل الفواتير الأوفلاين (Offline Sales Queue)**:
   - عند انقطاع الإنترنت، تحفظ الفاتورة محلياً ويتم إنتاج رقم تسلسلي مؤقت.
3. **إعادة المزامنة التلقائية (Auto-Sync)**:
   - عند عودة الاتصال، يتم رفع الفواتير المخزنة محلياً إلى `POST /api/orders/bulk-sync`.

---

## 📄 7. خطة الاختبار والتحقق (Verification & Testing Checklist)

يجب على المطور/الأيجنت الجديد إجراء التثبت التالي بعد أي تعديل:

1. **الاختبار البرمجي (Static & Build Checks)**:
   ```bash
   cd /home/elwens/Desktop/POS/flutter_pos_app
   flutter analyze
   flutter build apk --debug
   ```
2. **اختبار التشغيل المباشر على المحاكي**:
   - إرسال `Hot Restart` (`R`) والتأكد من عدم وجود أخطاء Exception في Log.
   - التقاط لقطات شاشة باستخدام `adb` للتأكد من المظهر والتفاعل:
   ```bash
   adb -s emulator-5554 shell screencap -p /sdcard/screen.png
   adb -s emulator-5554 pull /sdcard/screen.png ./screen.png
   ```
3. **اختبار الدورة المالية الكاملة (End-to-End Cycle)**:
   - تسجيل الدخول بكاشير -> فتح وردية بـ 500 ريال -> إنشاء طلب وتحديد طاولة -> إتمام الدفع طباعة الفاتورة -> إغلاق الوردية والتحقق من التقرير في الباك إند.

---

## 📌 8. ملخص الإرشادات للأيجنت المكمل (Instructions for Next Agent)

1. **حافظ على اتجاه الصفحة RTL واللغة العربية** في جميع الشاشات الجديدة.
2. **استخدم `_apiClient.dio`** لجميع طلبات HTTP الجديدة وتجنب استدعاء طرق غير معرفة مباشرة على `_apiClient`.
3. **تأكد من wrapping زِر القائمة Drawer بـ `Builder`** دائماً في أي `Scaffold` جديدة لتجنب خطأ `Scaffold.of()`.
4. **تأكد من فحص `flutter analyze`** بعد أي إضافة لتجنب إيقاف التطبيق.

---
**جاهز للتنفيذ والمتابعة بواسطة أي agent آخَر.**
